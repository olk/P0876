\abschnitt{representing \emph{main} and thread's \emph{entry-fn} as fiber}\lable{representation}

\zs{Note, that \cpp{m} represents the suspend \main as a fiber! This is a nice
feature because \main and thread's \entryfn\xspace are handled equivalent to the
fiber that were explicitly created.}\\
